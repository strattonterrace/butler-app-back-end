[pytest]
DJANGO_SETTINGS_MODULE = butler_api.settings.development
python_files = test_*.py *_test.py
addopts = -ra --strict-markers --tb=short
markers =
    unit: fast, no DB
    integration: hits the database / multi-app
    edge_case: edge-case / error-path coverage
testpaths = tests
